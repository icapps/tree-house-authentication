import PassportAuthentication from '../src/lib/passport.authentication';
import { localStrategyConfig, onLocalStrategy } from './lib/passport-authentication.config.test';

const user = {
  password: 'myPassword',
};

describe('Passport Authentication', () => {
  it('Login via Passport local strategy', () => {

  });
});
